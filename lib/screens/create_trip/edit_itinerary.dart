import 'package:flutter/material.dart';

class EditItinerary extends StatelessWidget {
  const EditItinerary({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Edit Itinerary Page")),
    );
  }
}
